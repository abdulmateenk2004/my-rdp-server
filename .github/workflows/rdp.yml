name: High Speed Free RDP
on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
    - name: Download Ngrok Binary
      run: |
        powershell -Command "Invoke-WebRequest -Uri 'https://bin.equinox.io/c/b34edq65oax/ngrok-stable-windows-amd64.zip' -OutFile 'ngrok.zip'"
        powershell -Command "Expand-Archive -Path 'ngrok.zip' -DestinationPath '.'"
    - name: Start Ngrok Connection
      run: |
        .\ngrok.exe authtoken ${{ secrets.NGROK_AUTH_TOKEN }}
        start-process .\ngrok.exe -ArgumentList "tcp 3389"
    - name: Maintain Server Loop
      run: |
        powershell -Command "$seconds = 21600; while ($seconds -gt 0) { Write-Output 'RDP Active...'; Start-Sleep -Seconds 60; $seconds -= 60 }"
